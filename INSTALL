You should compile fsdump with an up-to-date Go Compiler,
which is available at https://code.google.com/p/go/

(source as well as pre-compiled binaries are available)


If you have all dependencies installed, install with:
	$make
	$make install
